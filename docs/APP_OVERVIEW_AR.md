# نظرة عامة على التطبيق - دورة الذكاء الاصطناعي المكثفة

> **الإصدار:** 1.0
> **تاريخ التحديث:** يناير 2026
> **المنصة:** تطبيق ويب React + Supabase

---

## المحتويات

1. [مقدمة](#مقدمة)
2. [البنية التقنية](#البنية-التقنية)
3. [هيكل المشروع](#هيكل-المشروع)
4. [الصفحات الرئيسية](#الصفحات-الرئيسية)
5. [نظام المصادقة](#نظام-المصادقة)
6. [نظام الترجمة](#نظام-الترجمة)
7. [قاعدة البيانات](#قاعدة-البيانات)
8. [المكونات المتميزة](#المكونات-المتميزة)
9. [لوحة التحكم الإدارية](#لوحة-التحكم-الإدارية)
10. [التكوين والنشر](#التكوين-والنشر)

---

## مقدمة

### الهدف من التطبيق

تطبيق ويب متكامل لدورة تدريبية مكثفة في الذكاء الاصطناعي وهندسة الطلبات (Prompt Engineering). يُمكّن المستخدمين من:

- 📚 التسجيل في الدورة التدريبية
- 👤 إنشاء حساب والوصول إلى المواد التعليمية
- 📥 تحميل ملفات الدورة (PDF وغيرها)
- 🌐 التنقل بين اللغتين العربية والإنجليزية

### الميزات الرئيسية

| الميزة | الوصف |
|--------|--------|
| **دعم RTL** | دعم كامل للكتابة من اليمين لليسار للعربية |
| **الرسوم المتحركة** | تأثيرات بصرية متقدمة مع Framer Motion |
| **التصميم الزجاجي** | تصميم Glassmorphism حديث |
| **الحماية** | نظام مصادقة آمن مع Supabase |
| **لوحة تحكم** | إدارة كاملة للمحتوى والتسجيلات |

---

## البنية التقنية

### مكدس التقنيات (Tech Stack)

```
┌─────────────────────────────────────────────────────────────┐
│                        الواجهة الأمامية                        │
├─────────────────────────────────────────────────────────────┤
│  React 18.3.1          │  مكتبة واجهة المستخدم              │
│  TypeScript 5.8.3      │  الأمان النوعي                     │
│  Vite 5.4.19           │  أداة البناء السريعة               │
│  Tailwind CSS 3.4.17   │  تنسيق CSS                        │
│  Framer Motion 12.23   │  الرسوم المتحركة                   │
│  React Router 6.30.1   │  التوجيه                           │
│  TanStack Query 5.83   │  إدارة حالة الخادم                 │
├─────────────────────────────────────────────────────────────┤
│                        الخلفية                              │
├─────────────────────────────────────────────────────────────┤
│  Supabase              │  قاعدة بيانات PostgreSQL           │
│  Edge Functions        │  دوال الخادم                       │
│  Storage               │  تخزين الملفات                     │
│  Auth                  │  المصادقة                          │
└─────────────────────────────────────────────────────────────┘
```

### مكتبات واجهة المستخدم

- **shadcn/ui**: مكونات Radix UI جاهزة (+40 مكون)
- **Lucide React**: مكتبة الأيقونات
- **Embla Carousel**: شرائح عرض المحتوى
- **Recharts**: الرسوم البيانية
- **Sonner**: إشعارات Toast
- **React Hook Form + Zod**: إدارة النماذج والتحقق

---

## هيكل المشروع

```
prompt-craft-launchpad/
├── public/
│   └── translations/          # ملفات الترجمة JSON
│       ├── ar.json           # الترجمة العربية
│       └── en.json           # الترجمة الإنجليزية
│
├── src/
│   ├── components/           # مكونات React
│   │   ├── ui/              # مكونات shadcn/ui الأساسية
│   │   ├── premium/         # مكونات متقدمة (Glassmorphism)
│   │   └── admin/           # مكونات لوحة التحكم
│   │
│   ├── pages/               # صفحات التطبيق
│   │   ├── Index.tsx        # الصفحة الرئيسية
│   │   ├── Auth.tsx         # تسجيل الدخول
│   │   ├── Enrollment.tsx   # التسجيل في الدورة
│   │   ├── Dashboard.tsx    # لوحة تحكم المستخدم
│   │   ├── Admin.tsx        # لوحة التحكم الإدارية
│   │   └── ...              # صفحات أخرى
│   │
│   ├── contexts/            # سياقات React
│   │   ├── AuthContext.tsx  # المصادقة
│   │   └── SoundContext.tsx # المؤثرات الصوتية
│   │
│   ├── context/
│   │   └── TranslationContext.tsx  # الترجمة
│   │
│   ├── hooks/               # خطافات React المخصصة
│   │   ├── useCourseMaterials.ts
│   │   ├── useBenefits.ts
│   │   ├── useTranslation.ts
│   │   └── ...
│   │
│   ├── integrations/
│   │   └── supabase/        # إعداد Supabase
│   │       ├── client.ts    # عميل Supabase
│   │       └── types.ts     # أنواع TypeScript
│   │
│   ├── lib/                 # دوال مساعدة
│   │   ├── animations.ts    # إعدادات الرسوم المتحركة
│   │   ├── confetti.ts      # تأثيرات الاحتفال
│   │   ├── validations.ts   # مخططات التحقق
│   │   └── utils.ts         # دوال عامة
│   │
│   ├── App.tsx              # المكون الجذري
│   ├── main.tsx             # نقطة الدخول
│   └── index.css            # الأنماط العامة
│
├── supabase/
│   ├── migrations/          # ترحيلات قاعدة البيانات
│   └── functions/           # Edge Functions
│       ├── download-material/
│       └── health-check/
│
├── docs/                    # التوثيق
├── package.json
├── vite.config.ts
├── tailwind.config.ts
└── tsconfig.json
```

---

## الصفحات الرئيسية

### خريطة المسارات (Routes)

| المسار | الحماية | الوصف |
|--------|---------|--------|
| `/` | عام | الصفحة الرئيسية (Landing Page) |
| `/auth` | عام | تسجيل الدخول وإنشاء الحساب |
| `/enrollment` | عام | نموذج التسجيل في الدورة |
| `/dashboard` | مصادق | لوحة تحكم المستخدم |
| `/admin` | مسؤول | لوحة التحكم الإدارية |
| `/contact` | عام | صفحة التواصل |
| `/privacy` | عام | سياسة الخصوصية |
| `/terms` | عام | الشروط والأحكام |
| `/faq` | عام | الأسئلة الشائعة |
| `/glossary` | عام | قاموس المصطلحات |

### الصفحة الرئيسية (Index)

تتكون من الأقسام التالية:

```
1. Hero Section          - القسم الرئيسي مع CTA
2. CoursePhilosophy     - كيف تعمل الدورة
3. TargetAudienceSection - لمن هذه الدورة
4. CourseBreakdown      - برنامج الأيام الخمسة
5. Day5OutcomeSection   - مخرجات اليوم الخامس
6. TestimonialsSection  - آراء المشاركين
7. CTASection           - دعوة للتسجيل
```

### لوحة تحكم المستخدم (Dashboard)

- **الملف الشخصي**: عرض بيانات المستخدم
- **مواد الدورة**: تحميل ملفات الأيام الخمسة
- **الموارد الإضافية**: دليل الدورة وقوالب الطلبات

---

## نظام المصادقة

### سياق المصادقة (AuthContext)

```typescript
interface AuthContextType {
  user: User | null;           // المستخدم الحالي
  session: Session | null;     // الجلسة
  loading: boolean;            // حالة التحميل
  signUp: (...) => Promise;    // التسجيل
  signIn: (...) => Promise;    // تسجيل الدخول
  signOut: () => Promise;      // تسجيل الخروج
  resetPassword: (...) => Promise; // إعادة تعيين كلمة المرور
}
```

### تدفق التسجيل

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  نموذج       │ ──▶ │  التحقق من   │ ──▶ │  إنشاء      │
│  التسجيل     │     │  التسجيل     │     │  الحساب     │
└──────────────┘     └──────────────┘     └──────────────┘
                            │
                            ▼
                     ┌──────────────┐
                     │  ربط        │
                     │  التسجيل    │
                     │  بالمستخدم   │
                     └──────────────┘
```

### حماية المسارات

```tsx
// مثال على حماية مسار
<Route path="/dashboard" element={
  <ProtectedRoute>
    <Dashboard />
  </ProtectedRoute>
} />

// مسار يتطلب صلاحيات المسؤول
<Route path="/admin" element={
  <ProtectedRoute requireAdmin={true}>
    <Admin />
  </ProtectedRoute>
} />
```

---

## نظام الترجمة

### الإعداد

يدعم التطبيق اللغتين:
- **العربية (ar)**: اللغة الافتراضية مع دعم RTL
- **الإنجليزية (en)**: مع دعم LTR

### استخدام الترجمة

```tsx
import { useTranslation } from '@/hooks/useTranslation';

const MyComponent = () => {
  const { t, tArray, currentLanguage, toggleLanguage } = useTranslation();

  return (
    <div>
      <h1>{t('heroTitle')}</h1>
      <ul>
        {tArray('ctaFeatures').map((feature, i) => (
          <li key={i}>{feature}</li>
        ))}
      </ul>
    </div>
  );
};
```

### هيكل ملفات الترجمة

```json
{
  "heroTitle": "استخدم الذكاء الاصطناعي استخدامًا صحيحًا...",
  "enrollmentForm": {
    "firstName": "الاسم الأول",
    "lastName": "اسم العائلة",
    "experience": {
      "label": "مستوى الخبرة",
      "options": {
        "beginner": "مبتدئ"
      }
    }
  },
  "day1Topics": [
    "كيف تعمل أدوات الذكاء الاصطناعي",
    "تحويل الهدف إلى طلب واضح"
  ]
}
```

---

## قاعدة البيانات

### جداول Supabase

| الجدول | الوصف |
|--------|--------|
| `enrollments` | بيانات التسجيل في الدورة |
| `profiles` | ملفات المستخدمين الشخصية |
| `course_access` | صلاحيات الوصول للدورة |
| `course_materials` | مواد الدورة للتحميل |
| `course_days` | محتوى أيام الدورة |
| `benefits` | مزايا الدورة |
| `testimonials` | آراء المشاركين |
| `faqs` | الأسئلة الشائعة |
| `course_settings` | إعدادات الموقع |
| `target_audience` | الجمهور المستهدف |
| `instructor_checklist_items` | قائمة مهام المدرب |
| `instructor_session_notes` | ملاحظات جلسات المدرب |

### دوال RPC

```sql
-- التحقق من حالة التسجيل
check_enrollment_status(p_email)

-- ربط التسجيل بالمستخدم
link_enrollment_to_user(p_email, p_user_id)

-- التحقق من صلاحيات المسؤول
is_admin(_user_id)
```

### Edge Functions

| الدالة | الوصف |
|--------|--------|
| `download-material` | تحميل آمن للمواد التعليمية |
| `health-check` | فحص صحة الخدمة |

---

## المكونات المتميزة

### Premium Components

موجودة في `/src/components/premium/`:

| المكون | الوصف |
|--------|--------|
| `GlassCard` | بطاقة بتأثير الزجاج المصنفر |
| `MagneticButton` | زر يتحرك نحو المؤشر |
| `TiltCard` | بطاقة بتأثير الميل ثلاثي الأبعاد |
| `AnimatedBackground` | خلفية متحركة (mesh, particles, aurora) |
| `Floating3DElements` | عناصر ثلاثية الأبعاد طافية |
| `CommandPalette` | لوحة أوامر (Cmd+K) |
| `CustomCursor` | مؤشر مخصص |
| `ScrollProgress` | شريط تقدم التمرير |
| `SkeletonLoader` | هيكل تحميل للمكونات |
| `PageTransitionWrapper` | انتقالات الصفحات |

### استخدام المكونات

```tsx
import { GlassCard, MagneticButton } from '@/components/premium';

const MySection = () => (
  <GlassCard variant="gradient" glow>
    <h2>عنوان</h2>
    <MagneticButton variant="primary" size="lg" glow>
      اضغط هنا
    </MagneticButton>
  </GlassCard>
);
```

---

## لوحة التحكم الإدارية

### التبويبات المتاحة

| التبويب | الوظيفة |
|---------|---------|
| **التسجيلات** | إدارة طلبات التسجيل |
| **الأيام** | تحرير محتوى أيام الدورة |
| **المواد** | رفع وإدارة ملفات الدورة |
| **الآراء** | إدارة آراء المشاركين |
| **الأسئلة** | تحرير الأسئلة الشائعة |
| **المزايا** | تعديل مزايا الدورة |
| **الجمهور** | تحديد الجمهور المستهدف |
| **المحتوى** | تحرير نصوص الموقع |
| **الإعدادات** | إعدادات الدورة العامة |
| **المدرب** | لوحة تقدم المدرب |

### حالات التسجيل

```typescript
type EnrollmentStatus =
  | 'NEW'           // جديد
  | 'CONTACTED'     // تم التواصل
  | 'CONFIRMED'     // مؤكد
  | 'ACCESS_GRANTED' // لديه وصول
  | 'REJECTED'      // مرفوض
  | 'ARCHIVED';     // مؤرشف
```

---

## التكوين والنشر

### متغيرات البيئة

```env
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id
```

### أوامر التطوير

```bash
# تشغيل بيئة التطوير
npm run dev

# بناء الإنتاج
npm run build

# معاينة البناء
npm run preview

# فحص الكود
npm run lint

# تشغيل الاختبارات
npm run test
```

### تحسينات البناء

```typescript
// vite.config.ts
build: {
  rollupOptions: {
    output: {
      manualChunks: {
        vendor: ['react', 'react-dom', 'react-router-dom'],
        supabase: ['@supabase/supabase-js'],
        ui: ['@radix-ui/react-*'],
        query: ['@tanstack/react-query'],
      },
    },
  },
}
```

### النشر على Supabase

```bash
# ربط المشروع
supabase link --project-ref YOUR_PROJECT_REF

# نشر Edge Functions
supabase functions deploy download-material
supabase functions deploy health-check

# تطبيق الترحيلات
supabase db push --use-migrations
```

---

## الملحقات

### هيكل شجرة المزودين (Provider Tree)

```
<ErrorBoundary>
  <QueryClientProvider>
    <TooltipProvider>
      <Toaster />
      <Sonner />
      <TranslationProvider>
        <AuthProvider>
          <SoundProvider>
            <BrowserRouter>
              <CustomCursor />
              <ScrollProgress />
              <PageTransition>
                <Suspense>
                  <AnimatedRoutes />
                </Suspense>
              </PageTransition>
              <AccessWarningDialog />
            </BrowserRouter>
          </SoundProvider>
        </AuthProvider>
      </TranslationProvider>
    </TooltipProvider>
  </QueryClientProvider>
</ErrorBoundary>
```

### دعم إمكانية الوصول (Accessibility)

- احترام تفضيل تقليل الحركة (`prefers-reduced-motion`)
- أدوار ARIA للأقسام الرئيسية
- تسميات توضيحية للأزرار والروابط
- دعم التنقل بلوحة المفاتيح

### الأمان

- التحقق من المدخلات باستخدام Zod
- تنظيف البيانات قبل الإرسال
- حماية المسارات المصادق عليها
- دوال Edge آمنة للتحميل

---

## المراجع

- [توثيق React](https://react.dev)
- [توثيق Supabase](https://supabase.com/docs)
- [توثيق Tailwind CSS](https://tailwindcss.com/docs)
- [توثيق Framer Motion](https://www.framer.com/motion/)
- [توثيق shadcn/ui](https://ui.shadcn.com/)

---

> **ملاحظة:** هذا التوثيق يعكس حالة التطبيق الحالية ويجب تحديثه عند إجراء تغييرات جوهرية.
